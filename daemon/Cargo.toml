[workspace]
members = [
    "storage",
    "blacklist",
    "config",
    "ipc-protocol",
    "inference",
    "distiller",
    "capture-win",
    "merkwerk-daemon",
]
resolver = "2"

[workspace.package]
edition = "2021"
version = "0.0.0"
